<% include _header.ejs %>

  <body>
    <h1>
      <%= title %>
    </h1>
    <p>Join our guestbook!</p>
    <a href='/auth/entry'>Click here to sign the guest book</a>

    <div class='row'>
      <% for(var i=0; i<data.length; i++) {%>
        <div class='col-md-4'>
          <p class='lead'>
            <p>
              <%= data[i].review %>
            </p>
            <p>
              by:
              <%= data[i].username %>
            </p>
          </p>
        </div>

        <% } %>
    </div>
